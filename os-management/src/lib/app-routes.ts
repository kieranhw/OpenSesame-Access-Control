export enum AppRoute {
    // public
    LOGIN = "/login",
    SETUP = "/setup",
    // private
    HOME = "/",
    ENTRY_DEVICES = "/entry-devices",
    ACCESS = "/access",
    CREDENTIALS = "/credentials",
    CLIENTS = "/clients",
    SETTINGS = "/settings",
    PAIRING = "/pairing",
}