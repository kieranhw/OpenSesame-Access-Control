export enum AppRoute {
    // public
    LOGIN = "/login",
    SETUP = "/setup",
    // private
    HOME = "/",
    ENTRIES = "/entries",
    ACCESS = "/access",
    CLIENTS = "/clients",
    SETTINGS = "/settings",
}